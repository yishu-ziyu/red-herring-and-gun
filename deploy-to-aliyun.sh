#!/usr/bin/env bash
# Retired. This used to reset the server checkout to origin main.
echo "Retired. Live deploy is: ./ops.sh deploy --yes" >&2
echo "That command ships the current workspace (dev), not origin main." >&2
exit 1
