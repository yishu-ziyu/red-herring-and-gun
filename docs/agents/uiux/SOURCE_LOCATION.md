# agency-agents 位置

| 位置 | 说明 |
|------|------|
| **本机副本** | `vendor/agency-agents/`（完整 clone；已在 `.gitignore`，不进提交/推送） |
| **产品包装** | `docs/agents/uiux/`（wrapper + 精简 source-*.md，可随产品改动提交） |
| 当初临时 clone | `/tmp/agency-agents`（重启可能丢，勿依赖） |

上游：https://github.com/msitarzewski/agency-agents

规则：只 commit 产品改动；不要 `git add vendor/`。
