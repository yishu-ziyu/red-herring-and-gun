# 剩余 6 项 · 影响 + 阻塞 + 优先级矩阵

> 重新审视时发现：之前标 "可选" 过于轻描淡写。
> 实际影响分为三层：**用户可见**、**评委可见**、**业务/合规**。

## 总览表

| # | 项目 | 我之前标的状态 | 重新评估 | 影响等级 | 谁受影响 |
|---|------|---------------|---------|---------|---------|
| 1 | P1 → Mission Control UI 接入 | 可选 | **必做** | 🔴 高 | 用户 + 评委 |
| 2 | 报告 URL 永久路由 `/r/:caseId` | 待拍板 | **必做** | 🟡 中 | 评委（SEO） + 业务 |
| 3 | ClaimReviewBadge UI 组件 | 可选 | **必做** | 🟡 中 | 用户 + 评委 |
| 4 | 学术付费 API 选型 | 待拍板 | 拍板后做 | 🟢 低 | 学术场景 |
| 5 | 编辑室邀请制 / 审批人上限 | 待拍板 | 拍板后做 | 🟢 低 | 商业模式 |
| 6 | 多语言首期范围 | 待拍板 | 拍板后做 | 🟢 低 | 产品优先级 |

---

## 1. 🔴 P1 → Mission Control UI 接入（必做）

### 影响的护城河
- **护城河 6：Mission Control 实时可视化 Agent 流** ← 完全没做

### 实际状况
- P1 整波 6 个模块（KPA / Kialo 树 / 引用溯源 / 来源信誉 / 谬误诊断 / 盲点视图）**纯逻辑完成，63 测试 PASS**
- 但**用户看不到任何一项**——它们都还没接到 UI 上
- 现在用户提交 claim 后，只看到 P0 的 `求证：网传…，经核查` + 证据链

### 为什么不接
- 原话："jsdom 环境问题（plan §3 风险点）"
- 实际影响：4 个 pre-existing React 测试文件失败（`window is not defined`）——**这是 vitest 配置问题，不是逻辑问题**
- 业务影响：核心差异化能力（6 个护城河中的 1 个）完全没展示

### 建议
- **优先级：高**
- **不依赖任何拍板**，可以独立完成
- 接入量：6 个组件 + Mission Control 主面板扩展
- 预计 1-2 周

---

## 2. 🟡 报告 URL 永久路由 `/r/:caseId`（必做）

### 影响的护城河
- **护城河 7：ClaimReview JSON-LD 兼容** ← 已生成 JSON-LD，但 URL 字段缺失
- 业务：SEO 索引、长链接分享、引用追溯

### 实际状况
- `claimReview.ts` 已支持 `url` 字段注入
- 但后端 SSE payload 不传 `url`，前端 `ConclusionDockV3.tsx` 也不路由
- Google 2025-06 下线 Rich ClaimReview 富文本展示，但保留索引入口 → 需要稳定 URL

### 建议
- **优先级：中**
- 涉及 SEO 决策（路由规则 / URL 格式）
- 选项 A：短期哈希 `/r/{hash}`（无后端存储）
- 选项 B：长期 ID 路由 `/r/{caseId}`（需后端 case 表）
- **A 可立即开工**（不依赖拍板）；**B 需要你拍板**

---

## 3. 🟡 ClaimReviewBadge UI 组件（必做）

### 影响的护城河
- **护城河 7：ClaimReview JSON-LD 兼容** ← 后端已生成，前端没消费

### 实际状况
- `claimReviewScript.ts` 已有 8 测试覆盖 DOM 注入
- `claimReviewStream.ts` 已有 5 测试覆盖 SSE 桥接
- **`ClaimReviewBadge.tsx` 组件未创建**——`「复制 JSON-LD」按钮`、`<script>` 注入到 `<head>`、Rich Results Test 链接 都没接入

### 建议
- **优先级：中**
- 不依赖任何拍板
- 接入量：1 个组件 + ConclusionDockV3 加按钮
- 预计 1-2 天

---

## 4. 🟢 学术付费 API 选型（拍板后做）

### 影响的护城河
- **护城河 1（部分）：P2-1 学术通道** 已实现纯逻辑，待接真实 API

### 实际状况
- `academicSearch.ts` 是 mock 实现，DOI 去重 + Consensus 度量都已就绪
- 待接：Crossref（免费）vs Semantic Scholar（限速 1 req/sec）
- 涉及预算：Crossref 完全免费；Semantic Scholar 免费但限速

### 建议
- **优先级：低**（MVP 不需要真实数据就能 demo）
- 拍板选项：先用 Crossref 免费 + 缓存，结果落 `~/.gun/academicCache.json`
- 影响范围：仅"学术研究模式"用户（已通过 P2-5 mode 参数互斥隔离）

---

## 5. 🟢 编辑室邀请制 / 审批人上限（拍板后做）

### 影响的护城河
- **护城河（商业化）** ← B2B 入口，与 MVP 验证无关

### 实际状况
- P2-5 `intakeMode.ts` 已有 `newsroom` mode 配置（allowBatch + threshold 50）
- 待补：邀请制、批量审批人上限、权限模型

### 建议
- **优先级：低**（黑客松阶段不做也 OK）
- 拍板选项：完全邀请制 / 半开放（注册即可用，限额 5 case/天）/ 全开放
- 不影响 MVP 验证

---

## 6. 🟢 多语言首期范围（拍板后做）

### 影响的护城河
- **护城河（国际化）** ← 与中文市场护城河不直接相关

### 实际状况
- P2-6 `bilingualNormalize.ts` 已有 4 档语言检测 + alignKey 对齐（17 测试 PASS）
- 但只是后端规范化层
- 待补：UI i18n 框架（react-intl / i18next 选型）

### 建议
- **优先级：低**（产品定位为中文市场）
- 拍板选项：
  - A：仅后端规范化（已就绪）
  - B：UI i18n 中英双语
  - C：全 i18n（en/zh/ja/ko）

---

## 修正建议

| 原状态 | 重新评估 | 行动 |
|--------|---------|------|
| #1 P1 UI 接入：可选 | **🔴 必做** | 立即开工，不依赖任何决策 |
| #2 报告 URL：待拍板 | **🟡 必做（短期可 A 方案）** | 短期哈希路由不依赖拍板 |
| #3 ClaimReviewBadge：可选 | **🟡 必做** | 立即开工，1-2 天 |
| #4 学术 API：待拍板 | 🟢 拍板后 | 选 Crossref 免费方案即可开工 |
| #5 编辑室：待拍板 | 🟢 拍板后 | 黑客松阶段可跳过 |
| #6 多语言：待拍板 | 🟢 拍板后 | 黑客松阶段可跳过 |

## 立即可开工（不依赖任何拍板）

1. **P1 → Mission Control UI 接入**（1-2 周）
2. **报告 URL 短期哈希路由**（1-2 天）
3. **ClaimReviewBadge 组件**（1-2 天）

合计：2-3 周可全部完成。
