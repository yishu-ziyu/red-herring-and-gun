# 2026-09-04 · 首页不再套案件三栏壳

## 改了什么

`/` 不再包 `AppShell`。三栏只在 `/cases/:id`。

## 为什么

T17 为案件视图定了左列表 / 中线程 / 右卷宗。T19 把首页塞进同一只壳。结果落地页两侧是空栏，右侧还写着「整句判决 / 已完成」。这不是原来 mvp 首页，是脊柱重建时的骨架没拆干净。用户打开 5173 看到的就是这个。

## 结果

首页是一张纸。案件页仍是三栏。
