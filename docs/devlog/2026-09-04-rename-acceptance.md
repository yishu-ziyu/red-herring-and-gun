# 2026-09-04 · 「验收卡」改叫验收标准

## 改了什么

协议和 METHODOLOGY 不再用「验收卡」。`docs/evals/` 里放的是验收标准。

## 为什么

「卡」听起来像流程表格。要的不是填一张表，是有一份独立于实现的尺度，用来判断任务做没做完、完成到哪。有这份尺度，产品效率和 Agent 执行都会变强。

## 结果

`AGENTS.md`、`docs/METHODOLOGY.md`、NOTES、已有 evals 文案对齐。文件仍在 `docs/evals/`，不改目录。
