# 仓库拓扑写清，不搬家

用户要「标准软件工程的文件结构」，并要在过程里学全栈。

没把 `mvp/` 改名为 `apps/web`。T20 之前那是发布入口，搬家等于改 `ops.sh`。标准名字是单体仓库上的绞杀式迁移：生产仍是 `mvp/`，脊柱在 `packages/`。

用户随后裁定：提前把 `mvp/` 改成 `apps/`，发布路径一起改。已 `git mv mvp apps`，`ops.sh` 打包/测试/上传走 `apps/`，远端优先 `/opt/red-herring/apps`。T20 仍是切 `packages/`，不是这次改名。

全栈练习口令仍是：改用户看见的字，要问 prompt、清洗、快照、结果页四层。S1 那次是反例。
