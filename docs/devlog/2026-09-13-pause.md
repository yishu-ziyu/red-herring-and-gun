# 2026-09-13 停工交接

今日停工。产品不再改、不提交、不推远程、不部署。下面只记：本机已经有什么、还没做完什么、等人裁什么、以及容易误会成「已经做完」的事。

用户看得见的验收顺序没有变：对原句的直接回答 → 问题点 → 出处。测试绿不算用户路径走通；真实走查才算。

公网 `https://gun.yishuziyu.cn/` 仍是今早那一版，不含午后本机改动。

## 已在本机但未提交

这些已经写在本机工作区，机器项大多当时报过绿，但**没有 commit、没有 push**。分身空转后刚要提交，又被叫停。提交分身停下时，相关测试是 **315 绿 / 7 红**，没有 commit。公网看不到下面这些。

1. **思考区诚实与阶段过渡。** 思考区不再用按秒解锁的假三步（01/02/03）回声用户原文；收到原句后只说正在拆成可以单独核对的问题；命题出来后列出真实待查问题。拆问题结算后，找出处入场；作判断时核语境为进行中；新命题、来源胶囊、活动行错开淡入，存量不重闪；尊重减少动效。契约：`docs/evals/2026-09-13-thinking-honesty.md`。

2. **思考区等待秒数跟真实阶段对齐。** 命题出来之前始终一句真话，秒数从思考区挂上起用真实经过时间整秒累加。刚贴上是「正在把这句话拆成可以单独核对的问题（已等 N 秒）」；拆题回来、开始自证时换成「正在核对刚拆出的说法站不站得住（已等 N 秒）」；自证过了、命题出来后仍是「已拆出 N 个待查问题」。无新事件只改秒数，不用假装解锁。契约：`docs/evals/2026-09-13-thinking-wait-status.md`。

3. **多主张分别说。** 背景事实站住、另一条谣言站不住时，结论第一句按条写「……站得住；……站不住」，标签「有对有错」，不再整段写成「公开材料不支持这条说法」。没查的主张不计入、也不写成「证据反驳」。契约：`docs/evals/2026-09-13-multiclaim-leftover.md`。

4. **高压走查三条修复。** 追问第一句必须答这句追问，跨案知识库不得把别案记忆注入本案；长文只抽可核查断言、硬上限四条，「写进教科书」不单独成条、不单独判站不住（除非用户在问这个）；只贴打不开的链接时，全程常驻「链接打不开（可能需要登录），已按你输入的文字继续」，零命题时结论改成「没法从链接里读到要查的说法」。契约：`docs/evals/2026-09-13-pressure-fixes.md`。走查记录：`docs/evals/2026-09-13-pressure-walkthrough.md`。

5. **展示三条。** 完成态直答在前，来源条默认折叠、挂在结论第一句下面，点「已查验 N 个信息来源」才展开；解释不再漏出内部拼接残片；长城工程案不再把「解放军是保卫祖国的钢铁长城」这种比喻当该命题依据。契约：`docs/evals/2026-09-13-display-leftovers.md`。

6. **访客同一案追问快路径。** 没登录再问一句：胶囊仍先填再发；确认后不强迫登录。上一轮证据够答 → 不完整拆题、已核命题不再搜，结论答这句追问，来源标「依据来自刚才那一轮」；人物、日期、链接变了或跟已核命题对不上 → 只搜新的。没有可点开出处则仍走完整管道。登录有档案仍读服务端。契约：`docs/evals/2026-09-13-guest-followup-fast-path.md`（登录侧另见 `docs/evals/2026-09-13-followup-fast-path.md`）。

7. **若干简洁改动。** 正在按 `docs/evals/2026-09-13-simplify-and-ship.md` 收缝，不回退访客追问快路径、不重写脊柱。已动手的包括：去掉输入区与文案里重复的「链接打不开」；命题卡两套来源组件合成一份；思考区命题出来后停掉空转计时。这项没做完就被叫停。

今早发版通道本身已经打通并上过线（契约 `docs/evals/2026-09-13-deploy-channel.md`）。那一版在公网。午后上述改动**没有再部署**。

## 未做完

每条都写：问题是什么、做到哪、怎么做完、怎么验收。

### 1. 今天这批的提交与推远程

问题是什么：本机午后改动还在工作区，远程和公网都没有。  
做到哪：分身空转后刚要提交，被停工令叫停。提交分身停下时相关测试是 315 绿 / 7 红，**没有 commit、没有 push**。  
怎么做完：先处理下面第 2 条红测试；全绿后再按仓库惯例提交（写为什么，不写 Agent 署名，不提交密钥和大体积走查截图），再推到已跟踪分支。复工前须用户重新下令。  
怎么验收：`git log -1` 能看到新提交；`git status` 显示已推或与远程同步；提交说明不含 Agent 署名。契约：`docs/evals/2026-09-13-simplify-and-ship.md`。

### 2. 提交前留下的 7 条红测试

问题是什么：提交分身停下时，相关测试是 315 绿 / 7 红。当时没有记下这 7 条的名字。不能把「315 绿」当成已经可以提交。  
做到哪：没有 commit。产品代码按停工令不再改。  
怎么做完：先看这 7 条是不是今天这批引入的。是这批引入的再修；不是这批引入的，单独记，不要为了提交顺手改无关面。  
怎么验收：`cd apps && npm test` 全绿，或当时那条 vitest 全绿。测试绿仍只是机器证据；用户路径仍要真实走查。

### 3. 午后改动未再部署

问题是什么：公网仍是今早部署，用户在 `https://gun.yishuziyu.cn/` 看不到思考区诚实、等待秒数、多主张分别说、走查三条、展示三条、访客追问快路径。  
做到哪：今早发版已完成；午后本机改动未再走发布。  
怎么做完：第 1、2 条提交并推远程之后，再按既有发布脚本发一版。未提交就部署，等于把未收口的红测试和未审查 diff 送上公网。  
怎么验收：发版后公网标题与主脚本能对上新提交；真实打开公网站，走一遍「对原句的直接回答 → 问题点 → 出处」，能看到午后那批用户可见行为。`./ops.sh check` 绿是发布前机器门槛，不是用户路径验收。

### 4. 评分考试：第二次独立真跑、应停查题标定、部分模型钥匙 401

问题是什么：发版前那套「真实案例准不准」的考试刚重做过一轮，还不能当稳定闸门。对照成绩单已升到现行版本，数字来自一场约 22 分钟的真跑；假跑能打出对比行，但假跑分低所以闸门红，这是预期。DeepSeek / MiMo 部分调用 401，那场主要靠 MiniMax / StepFun 撑完。还缺：真人标定的「应停查」题、第二次独立真跑。  
做到哪：24 道算分题已标「该进入核查」；两道没有可核对对象的短题已退役；报告在 `docs/reports/2026-09-13-eval-exam-rebuild/`。未 commit。契约：`docs/evals/2026-09-13-eval-exam-rebuild.md`。  
怎么做完：钥匙 401 如实记、能配通再配，密钥不进 git；「应停查」题等人标真实样例，不捏造停查题凑分母；再独立真跑一次，数字进对照成绩单，不手改及格线、不用假跑分冒充真分。  
怎么验收：钥匙可用时 `npm run eval:gate` 跑完全集，打出四门新旧数字与差值；结构自检可用 `npm run eval:gate -- --fake`（只证明考试结构能用，分数不当对照成绩单）。相关考试单测绿。

### 5. 项目清单上仍在的：公共案例页

问题是什么：没有已核对的公共案例列表页和单条回放页。首页只有教学示例短语，不能当成已发布的真实调查。  
做到哪：从未做。验收矩阵里这两条一直空着。  
怎么做完：另开任务。先定哪些完成记录有权公开、来源仍可查、结论日期清楚、敏感信息已处理，再做列表与回放。回放必须零新调查、零扣额，并写清原调查日期。不要把教学示例改名成「真实案例」上线。  
怎么验收：人评能打开公共案例、看到原调查日期与原材料、回放不发新调查不扣额；虚构或教学数据必须标明，禁止发布成真实调查。现有契约未单列本条命令门，复工时另写验收契约。

### 6. 项目清单上仍在的：视频

问题是什么：用户传视频现在收不了，也查不了。路线图已记这个缺口，产品不能装作能查。  
做到哪：未做。  
怎么做完：另开任务。先定收什么、查什么、查不到怎么诚实说，再接入。未接入前，传了也要明说查不了，不要拿配了同样画面的二手帖当出处。  
怎么验收：人评。传视频后用户能看到「现在查不了视频」或查到的真实结果；不得把转述帖当成视频源。复工时另写验收契约。

### 7. 项目清单上仍在的：以图搜图

问题是什么：图片现在能读出图里的字并留下原图，但找不到这张图更早的出处。没有以图搜图。查到这一步应写原图没查到，不能把配了同样图的二手帖当成图源。  
做到哪：原图闸已在案件管道里；现网检索还没有以图搜图适配器。  
怎么做完：另开任务，接上能点到更早出处的以图搜图；接上之前保持「原图没查到」的诚实写法。  
怎么验收：人评加真实走查。有图时结论能写清原图有没有查到；查到的必须是更早出处，不是二手转帖。复工时另写验收契约。

### 8. 项目清单上仍在的：最窄屏追问占位被切

问题是什么：追问输入框的提示文字在最窄的 320 像素宽度下仍被切掉一行。375 / 414 宽度上修过，320 这一档还在切。  
做到哪：主路第二批记下「320px 占位仍切」，未再修。  
怎么做完：只改最窄宽度下提示文字的行高或高度，让第二行能完整看见，不借机改视觉风格。  
怎么验收：人评。在 320 像素宽度打开完成态追问框，提示文字两行都完整、不截断。机器项可补一条窄屏高度断言；用户路径仍以真机或浏览器走查看得到为准。

### 9. 项目清单上仍在的：断网再连

问题是什么：调查中途断网（不是整页刷新）再连上，会不会重复记账、会不会接回原来那一轮，没有验收过。刷新恢复已经做过；断网再连没有验。  
做到哪：未验。任务身份、停止按钮、刷新接回原轮已有。  
怎么做完：另开任务。先写清期望：不重复事件、能接回原轮或诚实标中断，再真机断网走查。不要在没验的情况下写「你可以切走，我们继续查」。  
怎么验收：人评加真实走查。断网再连不重复扣额、不重复事件；画面接回原轮或明确标已中断。复工时另写验收契约。

## 等人

这些不是改两行就能结束，要真人到场或产品负责人裁。

1. **真人「看不看得懂」访谈。** 机器项和任务单已齐（契约 `docs/evals/2026-09-13-comprehension.md`）。本机用浏览器模拟走过两轮：第一轮失败（来源链到打不开的占位域名）；第二轮三项通过，但那**不是真人**。还缺：没有技术背景的真实用户按任务单走完一遍，自己指出结论第一句在答原句、依据在哪、出处能打开。怎么验收：人评。记录表在 `docs/reports/2026-09-13-comprehension/`。模拟走查不得写成访谈已过。

2. **评分考试里「应停查」题的真人标定。** 现在算分题都标成该进入核查；资格闸「不完整输入应停在检索前」只有内部单测，考试集没有真人标定的停查样例。怎么验收：人标定真实样例后写入考试集；不拿捏造的停查题凑分母。见 `docs/evals/2026-09-13-eval-exam-rebuild.md`。

3. **部分模型钥匙 401。** DeepSeek / MiMo 部分调用不通。能配通再配；配不通就如实记，不编造成绩。密钥不进 git。

4. **访客追问快路径与展示措辞的观感。** 机器项当时报过绿。人评仍欠：结论第一句是否直接答这句追问、来源上「依据来自刚才那一轮」读不读得懂、来源能否点开。要在真实走查里看，不要只看测试绿。

## 不要再做的误会

1. **不要把本机未提交当成已经上线。** 公网仍是今早那版。用公网站验收午后改动，一定是错的。

2. **不要把「315 绿 / 7 红」当成可以提交。** 提交分身停下时没有 commit。7 条红测试先分清是不是这批引入的，再决定修不修。不要为绿灯放宽断言。

3. **不要把测试绿当成用户路径走通。** 产品路径仍是对原句的直接回答 → 问题点 → 出处。`cd apps && npm test` 和相关 vitest 是机器证据。真实走查才算用户路径。

4. **不要把模拟小白走查当成真人访谈。** 「看不看得懂」第二轮三项通过，执行者是浏览器模拟，不是没有技术背景的真人。

5. **不要把假跑考试分当成官方对照成绩。** 假跑能证明考试结构能用；对照成绩单必须来自真跑，不准手改及格线。

6. **不要回退访客追问快路径，也不要重写脊柱。** 简洁化只收今天已有改动的缝，不是另做一版产品。

7. **不要把清单上的公共案例页、视频、以图搜图、最窄屏占位、断网再连，写成今天已经在做或已经做完。** 它们一直在项目清单上，今天没有开工。停工后也不要借交接文档开做。

8. **不要继续改产品、不要 commit、不要 push、不要部署。** 本文件只记录。复工须用户重新下令。
