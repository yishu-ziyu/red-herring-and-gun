# 2026-09-04 · 走查不满意项

一句话：点发送后立刻看见原句；不写 0 条材料；四字章和分数不当卷宗主标题；过程不漏内部 id、不订在旧结论上；手机过程能读完。

## Change

1. 真立案跳进 `/cases/:id` 时，主栏先出现原句和等待圈，不再闪「正在打开案件 / 还没有案件」。
2. 材料数为 0 时不出现「已找到 0 条材料」。
3. 右侧卷宗和手机顶栏不再用「能信 / 不能信 / …」和巨大分数当主字；有结论时用结论第一句。
4. 过程步骤把 `added e4` 这类内部结果收成用户能懂的话。
5. 过程条只挂在当前这一轮，不出现在旧结论下面。
6. 手机过程折叠行完整可读。

## Not this

- 改首页排版
- 删右侧卷宗
- 把 claim 行小芯片也清掉（这次只改卷宗头和顶栏）
- 改 `ops.sh` / 删 `mvp/`

## Evaluator

```bash
npm test -w @rhg/web
```

浏览器：首页示例立案后第一帧有原句；`/cases/fx-done` 顶栏和卷宗头不是「不能信」；展开过程看不到 `e4`；`/cases/fx-followup` 过程条不在第一段旧结论下；375 宽过程行不被切碎。

## Goal / Hard bar / Improve

- Goal: 走查里双方都不满意的六步，用户路径上不再出现
- Hard bar: 测试绿；再走 5173 对应步骤，旧缺陷截图对不上
- Improve: none
