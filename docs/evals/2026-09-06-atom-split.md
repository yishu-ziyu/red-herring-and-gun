# 原子拆分不再靠关键词名单

## Change

- 删除 `forceCheckable.ts` 的正则名单，模型第一次填写就要填对类型和能不能查，并带原句位置。Evaluator：RUMOR-008 同类并列说法拆出两条以上，原句没说过的不出现；`grep -rn WEIBO|STANCE|RANT packages/core/src/text/claimAtom` 为空。
- 拆错可定位。Evaluator：`packages/core` 拆分测试用真实难例，跑 `npm test --workspace=packages/core -- decompose` 全绿；新增回归例来自 RUMOR-008 的全队与个别层级混淆。
- 不把检索失败藏成拆分问题。Evaluator：拆分输出与是否搜到东西无关，无来源也不改拆分结果。

## Not this

- 不改判决分数和名单分档，那是另一单元。
- 不为了过测试改 golden 期望值。
- 不另起第三套实现。`packages/core` 修完，线上版本（`mvp/server`）同步跟进同一条改动，各自测试通过。

## Evaluator

- 命令：`npm test --workspace=packages/core -- decompose claimAtom` 全绿。
- 人评：拿 5 条真实谣言看拆分，条条是原句里真实存在的判断，没有原句没说的内容，没有漏并列。

## 证据（2026-09-06 真模型实测，MiniMax）

改前同一句 9 次三种样子：拆成两条、合成一条、全部删掉。RUMOR-008 出现过并列压成一条，床垫位置盖在前十三个字。

- 关键词名单删除：`packages/core/src/text/claimAtom/forceCheckable*.ts` 已删，`grep` 为空。`decompose` 26 项通过。
- 位置校准：床垫位置回到床垫二字；原句一半字找不到的不要位置；`decompose` 26 项通过。
- 并列复核：有和与顿号多花一次小调用，合成一条的拆开，无标记的不花。单元 3 项，真模型第 5 次验证通过。
- 降温：填表走 temperature 0，写报告保留默认，开 thinking 的不带。新代码和线上版本同步。同一句再跑 3 次，次次两条。`core` 541 项、`mvp` 相关 48 项通过。
- 提示词：5 条标准谣言 4 条形状对，点图片会中毒被标不可查，加一段能力与风险断言规则后翻成因果可查，空调床垫不变。新代码和线上版本同步。

未结：第二遍检查错杀率没量；全删回去问用户要原话还没改；措辞小差别和数量歧义还在。

## 测量记录（只看数据）

### 基线（2026-09-06，规则版提示词 11 条零例子，降温后，能力规则加入前）

5 条标准谣言，真模型各跑 1 次：

- RUMOR-008 空调床垫：2 条事实可查，对。
- RUMOR-005 点图片中毒：2 条但全标预测不可查，错，后面无东西可查，漏网。
- RUMOR-011 红酒法国人：3 条事实因果可查，对（比预期 2 条更细，可接受）。
- RUMOR-002 扫码补贴：1 条，对，逾期没单列。
- RUMOR-012 疫苗自闭症：1 条因果，对，发烧没单列。

底数：形状 4.5/5，可查性 3.5/5。

### 第一轮后（加能力与风险断言一段）

- RUMOR-005 翻成 2 条因果可查，对。
- RUMOR-008 还是 2 条事实可查，对。

底数：形状 5/5，可查性 5/5（单次运行，模型有波动，以多次为准）。

### 提示词改写（2026-09-06，规则版 11 条零例子 → 5 条硬要求加 8 个完整例子）

学 FActScore：例子不讲规则。8 个例子就是 8 类坑：空调床垫拆、扫码限定语不拆、疫苗前提不拆、点图片能力断言、电动侧面合成、P图背景不拆、收购限定语不拆、保健品前提单列。

同一套 5 条再量 1 次：5 条形状全对，可查性全对。RUMOR-002 这次没把限定语单列，RUMOR-012 前提没单列，RUMOR-011 还是 3 条。

同一句再跑 3 次：次次两条，形状不再变。但单次耗时从 10 秒涨到 13 到 31 秒，例子吃 token，这是代价。

底数：形状 5/5，可查性 5/5，稳定性 3/3 同形。`decompose` 26 项通过。
