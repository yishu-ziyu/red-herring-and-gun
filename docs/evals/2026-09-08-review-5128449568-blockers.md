# 2026-09-08 PR #79 第五轮：Review 5128449568 三个 correctness blocker

## Change

按人工 Review `5128449568`（针对 HEAD `6e91bbb`，当前无更新 Review）把三个 correctness blocker 修完，继续留在 PR #79 同一分支，不开新 PR：

1. **Blocker 1**：checkable 但 unresolved / 方向无据 / 缺判词的 Claim 不得被硬结论偷偷判掉。overall true/false 可由其他有据 Claim 合法保留，但 repair 文本必须明确哪些 checkable Claim 尚未查清、未计入该判断。不读结论文本、不做 regex。
2. **Blocker 2**：Whole-Claim Audit 失败/超预算从 fail-open 改 fail-closed。Planning 的 `missingJustifications` 一旦产生先成为保守 gap；只有成功 Evaluation / authoritative re-evaluation 才能更新或关闭。已配置 audit caller 时，Planning/Evaluation 因失败或预算不足未完成要留下结构化 audit status / unresolved boundary，不静默清空。不新增自由循环或模型调用。
3. **Blocker 3**：建立共享的 direction-specific「有据」契约：true/trueish 由 supportingSources 支撑、false 由 contradictingSources 支撑（#74 alignment 先行）、related-only 永远不算、错桶 URL 不算该方向证据。merge guard（`demoteUnsourcedTrueFalse`）、`deriveOverallVerdict`、`applyConclusionGate`、publication repair 与 Snapshot 判词映射同用此语义。

## Not this

- 不重做 Planning/Evaluation 的语义与 schema；不新增 free loop、模型调用、Agent、关键词规则。
- 不改 UI、不改 Golden Path、不改 InvestigationSnapshotV1 schema、不改 source identity。
- 不碰旧 REAL artifacts（`final/real*`）、不碰 baseline.json 与 LegacyDesk 既有失败、不重跑 eval:gate（既有基线原因，等人裁；本修复属确定性 gate 逻辑，见上一轮先例）。
- 不启动 #54；不做 #53 收尾；不做 QA 平台建设（工作包 B 另立分支）。
- 不因本修复自动 merge；修完回复 Review 并 STOP 等人工复审。

## Evaluator

先建反例（修复前必须红），修复后同一反例必须绿，且既有回归不红：

- **E1（Blocker 1，Review 原样回归）**：管线反例 A（verifiable，verdict=false，contradictingSources=[alive URL]）+ B（verifiable，verdict=unverified，evidence=[]）+ composer verdictType=false 写「A、B 都不成立」→ overall false 保留；B 的 Snapshot judgment=unresolved；directAnswer/summary 含「B 尚未查清、未计入该判断」的边界句，不含「都不成立」原文。命令：`cd mvp && npx vitest run src/lib/casePipeline/runCasePipeline.wholeClaimAudit.test.ts`。
- **E2（Blocker 2，Review 原样回归）**：管线反例 Planning 输出 `missingJustifications` 桥接缺口 → Evaluation 返回 null（非法结构/失败）→ 最终硬 verdict 收为 unverified，缺口保留（`rumorStep.output.wholeClaimAudit.missingJustifications` 非空、`wholeClaimAudit.evaluationStatus="failed"`）。同型反例：deadline 令 Evaluation 无预算启动 → `evaluationStatus="skipped-budget"`，同样的收权与缺口保留。命令同 E1。
- **E3（Blocker 2 防误伤）**：audit 完整成功且 gap=[] 时，单命题正常硬 verdict 保留、composer 原文不被重建。命令同 E1。
- **E4（Blocker 3）**：单元级——`true + contradictingSources=[URL]` 不得算 sourced true（deriveOverallVerdict 不升 true、applyConclusionGate 收权）；`false + supportingSources=[URL]` 未对齐时收为 unresolved，经 #74 alignment 进 contradict 后可保留 false。merge/derive/gate 三处同语义（共享 `hasDirectionalBoundHttpUrl`）。管线级——true + contradict-only → 整句收 unverified、Claim judgment=unresolved、composer 硬结论文本被重建；false + support-only → alignment 后 false 保留。命令：`cd mvp && npx vitest run src/lib/wholeClaimAudit/wholeClaimAudit.test.ts src/lib/reportAssembly/assembleFinalReport.test.ts src/lib/casePipeline/runCasePipeline.wholeClaimAudit.test.ts`。
- **E5（全量门禁）**：根 `npm test` 全绿；根 `npm run build` exit 0；`cd mvp && npm test` 无新增失败（LegacyDesk 已知抖动按基线对照定性）；`cd mvp && npm run build` exit 0；`cd mvp/server && npx tsc --noEmit` exit 0。
- **E6（镜像一致）**：`mvp/server/src/lib/claimAtom/merge.ts` 与 `packages/core/src/text/claimAtom/merge.ts`、`mvp/server/src/lib/citationBinding.ts` 与 `packages/core/src/text/citationBinding.ts` 字节一致（`diff` 为空）。
- **E7（约束不回归）**：`git diff` 不含 Snapshot schema 文件（`investigation/schema.ts`）、UI 组件、关键词词表新增；`needsConstrainedConclusion`/`applyConclusionGate`/repair 全程无结论文本 regex。
- **E8（人评项，等人裁）**：Review 5128449568 的三条要求是否被忠实实现（不做字面执行检查，由人工复审裁决）；merge 授权；#79 是否可进入工作包 C 的 post-#78 复验。

机器项 E1–E7 全绿 + E8 人工裁决通过，本工作包才算完成。

## 实现与本轮结果（2026-09-08）

### 修复落点

- **Blocker 3（共享方向契约）**：`citationBinding.ts` 新增 `hasDirectionalBoundHttpUrl`（mvp/server 与 packages/core/src/text 两侧字节一致）。三处消费：
  - merge guard `demoteUnsourcedTrueFalse`（claimAtom/merge.ts，两侧镜像）：true 只认支撑桶、false 只认反证桶（`alignFalseEvidenceBuckets` 在 merge 内先行改桶），错桶/无源/related-only 一律收 unverified 补「待补证」；
  - `deriveOverallVerdict`（assembleFinalReport.ts）：`isSourcedTrueishVerdict` / `isSourcedFalseVerdict` 改用方向契约；
  - `applyConclusionGate`：`hasSourcedFalseVerdict` 改用方向契约；`verdictHasBoundHttpUrl`（并集）仅保留给「判词层完全无绑定 vs 有绑定材料」的区分（pre-liveness 礼让 reviewer、post-liveness 全灭检查），注释写明不再承担方向判断。
- **Blocker 1（未查清边界）**：`needsConstrainedConclusion` 新增第四触发条件——终态硬 verdict 但存在 unresolved/方向无据的 checkable 原子（`listUnresolvedCheckableAtoms`）→ 重建（规则 `hard-verdict-with-unverified-boundary`），overall 由其他有据 Claim 合法支撑时保留不降级；弱 verdict 的 `hasSourced` 判定同步改方向契约。`repairGatedConclusion` 重建文本加入「「X」尚未查清，未计入该判断。」边界（conclusion/summary/evidenceChain 层同步）。全程不读结论文本、无 regex。
- **Blocker 2（audit fail-closed）**：`auditUnresolvedGaps` 声明前移；Planning 成功后 `missingJustifications` 立即成为保守 gap 基线；Evaluation 返回 null → `evaluationStatus="failed"`，预算不足未启动 → `"skipped-budget"`，两态都写入结构化 `rumorStep.output.wholeClaimAudit` artifact 且缺口保留；成功 Evaluation / authoritative re-evaluation 才能更新或关闭（原有结算逻辑不变）。`WholeClaimAuditRun` 增加 `evaluationStatus` 可选字段（实现层 artifact，非 Snapshot schema）。

### 机器门禁（实际运行）

- 反例先行：修复前单测 9 红 + 管线 5 红（全部对应三个 blocker 的反例；其中 1 项防误伤用例红是新字段断言，1 项 false+support-only 对齐守卫本来就绿）。修复后同批全绿：wholeClaimAudit + assembleFinalReport + agentConfigs + wholeClaimAudit 管线 163 项通过。
- 根 `npm test`：605（core）+ 85（eval）+ 21（server）+ 83（web）全绿。
- 根 `npm run build`：exit 0。
- `cd mvp && npm test`：1085 passed / 1 skipped / 0 failed（本轮 LegacyDesk 未复现失败；该用例仍是已知抖动，不据此改口径）。
- `cd mvp && npm run build`：exit 0。
- `cd mvp/server && npx tsc --noEmit`：exit 0。
- 镜像：`claimAtom/merge.ts`、`citationBinding.ts` 两侧 `diff` 为空（E6）。
- `git diff` 不含 `investigation/schema.ts`、investigation/build.ts、任何 .tsx（E7）。
- `eval:gate` 未重跑：基线既有原因（§9 记录）与本轮无关，且全量 gate 需真实模型费用，未获本轮预算授权；信号同上一轮，等人裁。

### 新增回归清单（12 项）

单元（wholeClaimAudit.test.ts）：hasDirectionalBoundHttpUrl 2、applyConclusionGate 方向 3、needsConstrainedConclusion 未查清边界 3、repairGatedConclusion 未查清文本 1；assembleFinalReport.test.ts：derive 方向契约 1（既有 fixture 按新契约改桶）；agentConfigs.test.ts：mergeSubclaimVerdicts 方向 1。
管线（runCasePipeline.wholeClaimAudit.test.ts）：Blocker 1「A sourced-false + B unverified」1、Blocker 2「eval null / 超预算 / 成功防误伤」3、Blocker 3「true+contradict-only / false+support-only 对齐保留」2；harness 增加 deadline 注入。

### 遗留与人评

- E8 人评项（Review 三条要求的忠实性、merge 授权、是否进入 post-#78 复验）等人裁决。
- `needsConstrainedConclusion` 不检测「checkable 原子完全缺失判词」：`mergeSubclaimVerdicts` 对 kept atoms 无条件补 unverified 条目，装配后缺失判词在结构上不可达；unverified 条目已覆盖该语义。如未来出现绕过 merge 的路径需另立检查。
