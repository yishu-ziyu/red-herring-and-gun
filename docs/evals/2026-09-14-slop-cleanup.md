# 2026-09-14 松散热点清理

Date: 2026-09-14
来源：2026-09-14 代码松散度快照标出的克隆组与超阈值函数。只拆评测脚本与工具脚本里的重复/大函数，不碰产品主路径，不为刷分 minify / 删注释 / 把函数揉成一行。

对照快照（必须保留，数字来自 `measure.py`）：
- 啰嗦程度 verbosity = 0.23680271183085697
- 结构侵蚀 erosion = 0.6733775508401552
- 源码行 total_loc = 80831

## Change

针对工具标出的克隆组和圈复杂度/认知复杂度超阈值函数做结构清理后，用同一 `measure.py` 再测；啰嗦程度和结构侵蚀都低于上次快照；热点列表不再被同一批未处理的超阈值函数主导。

## Not this

- 删注释、minify、把大函数压成一行、把代码挪到测量忽略的目录、只改产品主路径却不动评测脚本热点。

## Evaluator

再跑一次 measure.py；对比两次 JSON 的 verbosity、erosion、total_loc。verbosity 必须下降，erosion 必须下降。若某项没降：说明根因（改错地方 / 热点质量在别的函数 / 再拆会藏结构），不要为过线去作弊。

机器项：`cd apps && npm test` 里与改动相关的测试仍绿（评测脚本若有测试就跑；至少保证改过的文件能编译/加载）。人评：不要求。

## Evidence

清理 diff + 第二次 measure.py JSON + 新旧对照表。
