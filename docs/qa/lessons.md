# QA 经验沉淀（lessons）

只在修复被独立验证通过后追加条目。每条包含：失败机制、被破坏的不变量、回归 ID、适用范围、不适用范围。

不进入本文件的内容：每次错误的流水账、「看到某个句子就答 X」的表面规律、未经验证的猜测。

格式：

```markdown
## <日期> <一句话标题>
- 失败机制：
- 被破坏的不变量：
- 回归 ID：
- 适用范围：
- 不适用范围：
```

（暂无条目——第一条将在 #79 合入并复验后按上述格式登记。）
