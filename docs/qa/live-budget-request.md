# LIVE 首批申请（2026-09-08）

状态：REQUESTED，尚未批准；实际 LIVE 调查次数 0。本轮先交离线修复与回放，停止等复审。

- 范围：两条历史诊断句各一次、充分支持正向对照一次、独立 Validator 保留变式一次。失败与重试均计入 4 次。
- 并发 1；每调查最多 40 次模型请求、80 次搜索请求；批次最多 160 / 320 次。每个 provider 请求与 retry/fallback 分别计数。
- 每调查墙钟 10 分钟、批次 40 分钟；浏览器每 session 至多 40 个动作。
- 拟用模型：MiniMax-M2.7-highspeed；独立质询 step-3.7-flash。其它模型 fallback 未申请。
- 搜索候选：360、AnySearch、Metaso、Tavily、Exa、MiniMax、StepFun。运行前冻结实际启用名单。
- 模拟器/评分器：本批不另开付费 API；当前开发会话费用不当成 LIVE 费用 0。
- 单次费用、账户实际费率、批次金额上限：unknown。申请批准明确调用额度；不把未知费用写成 0。
- 前置条件：#79 人工合入、独立 reference dossier 核对、候选前后端证据双绑定、额度与 deadline 可实际强制执行。现有 Promise.race 超时不保证取消在途计费请求；执行前必须关闭这个缺口或维持 BLOCKED，不能仅凭声明开跑。
- 本轮不实现付费执行器、不改线上 rewrite、不覆盖生产服务、不启动定时任务。
- 停止预算：每根因最多 2 次自动修复，本批最多 3 轮测量—修复—重验。

本文件为申请，不是授权记录。用户批准消息与后续实际账单/调用收据另行记录。
