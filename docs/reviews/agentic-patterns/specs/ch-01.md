# 第 1 章施工规范 · 提示词链

## status
verify-only

## 独立审查意见
生产主链是代码定序的拆题→闸门→检索→对照→收束，成立。
「没有可点开网址不得写成真或假」不是本章已兑现，归第 14/18 章。
Vite 中间件仍走 AgentRuntime，ADR-003 标成 Dev/eval。
不推翻 INDEX 5 分。

## Goal
确认 Express 生产入口仍走分步链。
产品代码零改动。

## Hard bar
runCasePipeline 顺序测试：只检索事实条，价值条进 nonVerifiableAtoms。
orchestrateHandler 与 orchestrateStreamHandler 都调用 runCasePipeline。

## 改哪些文件
无

## 测试
在 mvp/ 执行：`npx vitest run server/src/lib/casePipeline/runCasePipeline.test.ts -t "顺序：rumor → self-proof 过滤"`

## 明确不做
不迁 LangGraph。不改类型闸、bind、问法、eval。
