# 第 2 章施工规范 · 路由

## status
implement

## 独立审查意见
第 2 章原审查员卡在没有写入工具上，本规范由主会话按 INDEX 与 ch-02.md 补写。
类型闸没有第二意见：能核对的流传说法被标成立场就不检索。
自证不管类型。模型挂了会 fail-open，自信标错反而跳过。
补闸必须是纯函数，代码决定搜不搜。

## Goal
读起来像流传说法的原子，即使拆题写成 verifiable=false，仍进入检索。
纯价值 / 规范句仍不检索，报告仍是「立场型 / 不适用真/假判断」。

## Hard bar
原子「隔夜菜会致癌」被标 type=value、verifiable=false → 仍出现在 retrieveForAtoms 实际检索清单（经由本闸改回可核查或强制加入）。
原子「政府应该禁止隔夜菜」type=normative、verifiable=false → 不检索，报告该条「立场型 / 不适用真/假判断」。
原子「这种政策就是不管老百姓死活」type=value → 不检索。
现有 splitVerifiableAtoms 不变量测试仍绿。

## 改哪些文件
新建 `mvp/server/src/lib/claimAtom/forceCheckable.ts`（或同等纯函数）：对 verifiable===false 的原子，若像可核对流传说法则改回 verifiable=true、type 优先 fact 或 causal（含「导致」则为 causal）。
像流传说法的信号（可叠加，命中任一即可）：具体对象+判断（地名/机构/食品/药物/数字/「会/已经/决定/开通/致癌/免票/要建」）；微博级短句规则与 rumor_detector 提示词一致。
不像：应该/不应该、有意义/无意义、好/坏、纯骂、第一人称体验。
接线：`runCasePipeline.ts` 在 self-proof 之后、retrieveForAtoms 之前调用一次，写回 rumorStep.output.claimAtomTypes。
测试：`mvp/server/src/lib/claimAtom/` 新测或 agentConfigs.test 旁路。
禁止改 atomSearch.ts 选条（第 20 章）。禁止改 eval。

## 测试
`npx vitest run server/src/lib/claimAtom/` 及相关新测（在 mvp/）
`npx vitest run server/src/lib/casePipeline/runCasePipeline.test.ts`

## 明确不做
不让 LLM 自选下一条链。不另雇更强分类器。不上 LangGraph。
不改 FactChecker 回头改类型。不改 selectAtomsToSearch。
