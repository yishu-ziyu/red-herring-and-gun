# 第 3 章施工规范 · 并行化

## status
verify-only

## 独立审查意见
原报告路径主张基本站得住：五源并行、同原子 2–3 条 query 再并行、SSE 原子层排队、Phase 2 与因果两步并行、证据循环串行。
过满在把「没来源却写成能信」记在并行化头上，那是第 14、18 章。
漏：原子并行支是 `Promise.all` 不是 `allSettled`；生产被 `get360SearchForClaim` 兜住。
没有发现并行形状导致该查没查。维持 verify-only。

## Goal
执行员跑完已有测试且全部通过。
产品代码与 `ch-03.md` 均未被改动。

## Hard bar
`retrieveForAtoms` 只对可核查原子调用 searchOne。
sequential 模式 hooks 按原子成对出现。
多 query 合并按 URL 去重。
立场条进「立场型 / 不适用真/假判断」。

## 改哪些文件
无

## 测试
在 `mvp/` 下执行：
`npx vitest run server/src/lib/atomSearch.retrieve.test.ts server/src/lib/atomSearchQuery.test.ts server/src/lib/casePipeline/runCasePipeline.test.ts server/src/lib/reportAssembly/assembleFinalReport.test.ts server/src/lib/evidenceLoop/evidenceLoop.test.ts src/components/v3/ReportModal.test.tsx`

## 明确不做
不改产品代码。
不上 LangGraph 并行智能体。
不把 SSE 原子层改回 parallel。
不改 bind、选条、问法、fallback。
