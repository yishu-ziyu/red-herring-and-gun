# 第 4 章施工规范 · 反思

## status
verify-only

## 独立审查意见
四段笼子都在 `runCasePipeline.ts`：未证实才补搜、零增益不重判、有据之真加有假则整句「只能信一部分」、前序未支持却写成能信则降成「还查不清」。
过满：`reportReviewer` 对绝对公众摘要只记 warn，不改字。
正向判词不带来源也不触发循环，那是第 14、18 章。
类型闸归第 2 章。维持 verify-only。

## Goal
执行员跑完已有测试，确认四段笼子仍在。
产品代码零改动。

## Hard bar
`findLoopTargets` 无判词 → unverified 触发。
正向判词不带来源 → 不触发。
新证据 → `evidence-found` 且重跑 fact_checker。
两轮零新增 → `no-new-evidence` 且不重跑。
有据之真 + 假 → faceVerdict「只能信一部分」。
overclaim true + fact unverified → verdictType unverified。
crossExam 分歧降分不改主判词。

## 改哪些文件
无

## 测试
在 `mvp/` 下执行：
`npx vitest run server/src/lib/evidenceLoop/evidenceLoop.test.ts server/src/lib/reportReviewer.test.ts server/src/lib/reportAssembly/assembleFinalReport.test.ts server/src/lib/crossExam/crossExam.test.ts server/src/lib/casePipeline/runCasePipeline.test.ts`

## 明确不做
不引入无界自我反思。
不改 bind、merge、类型闸、findLoopTargets 排序、问法。
