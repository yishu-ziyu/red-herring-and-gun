# 第 5 章施工规范 · 工具使用

## status
verify-only

## 独立审查意见
生产是编排层先搜再注入，tools: []，不该打开 function calling。
过满：空结果走还查不清其实 bind 不改 verdict。归第 14/18 章。
不推翻 INDEX 5 分。

## Goal
验收：模型没有工具列表，现有检索单测仍绿。
产品代码零改动。

## Hard bar
atomSearch.retrieve：立场条不进 searchOne。
makeRunAgent 仍是 tools: []。
生产核查路径不传工具定义。

## 改哪些文件
无

## 测试
在 mvp/ 执行：`npx vitest run server/src/lib/atomSearch.test.ts server/src/lib/atomSearch.retrieve.test.ts server/src/lib/casePipeline/runCasePipeline.test.ts`

## 明确不做
不打开 function calling。不改 bind。不改类型闸。
