# 第 7 章施工规范 · 多智能体协作

## status
verify-only

## 独立审查意见
代码定顺序、JSON 交接、并行核查与信源、冲突只降分，成立。
过满：六张工单计数、角色失败「不会补能信/不能信」（fallback 在像辟谣时会写 false，归第 12 章）。
维持 verify-only。

## Goal
验收记录证明生产顺序与交叉复核不改判词仍在。
产品代码零改动。

## Hard bar
runCasePipeline 立场条不检索。
cross exam 分歧降分不重写判词。
AGENT_CONFIGS 六个 id 仍在。
生产拆题 schema 无 handoffTargets。

## 改哪些文件
无

## 测试
`npx vitest run server/src/lib/casePipeline/runCasePipeline.test.ts server/src/lib/crossExam/crossExam.test.ts server/src/lib/agentConfigs.test.ts server/src/lib/reportReviewer.test.ts`（在 mvp/）

## 明确不做
不补评委网格。不改 fallback。不上 Crew。
