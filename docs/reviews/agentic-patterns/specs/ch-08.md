# 第 8 章施工规范 · 记忆管理

## status
verify-only

## 独立审查意见
生产结案只 propose，开案不 searchAccepted。proposed 不进召回。
确认按钮归第 13 章。问法复用归第 9 章。
「不会把没来源写成能信」过满，归 bind。

## Goal
确认旧案不当本案证据。产品代码零改动。

## Hard bar
proposed 不进入 searchAccepted 测例仍绿。
runCasePipeline 只有 propose。
MemoryCandidatePanel 除自身外无 import（第 13 章会改 ResultView）。

## 改哪些文件
无

## 测试
`cd mvp && npx vitest run src/lib/semanticRecall.test.ts server/src/lib/memoryCandidateHandlers.test.ts server/src/lib/memoryCandidateGenerator.test.ts`

## 明确不做
不挂确认按钮。不上向量库。不改 bind。
