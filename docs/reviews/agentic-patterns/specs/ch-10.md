# 第 10 章施工规范 · 模型上下文协议 (MCP)

## status
verify-only

## 独立审查意见
内部 Case Pipeline 不靠 MCP。MiXer 入驻写明无认证。公网加鉴权会卡死握手。
Nginx 未反代 /mcp 是部署缺口，超出本章独占。维持 verify-only。

## Goal
已有 MCP 单测通过。本章不改产品代码。

## Hard bar
`npx vitest run server/src/lib/mixerMcp.test.ts` 四个用例全绿。
mcpHttpHandler 不读 Authorization。
runCasePipeline 不引用 MCP。

## 改哪些文件
无

## 测试
`cd mvp && npx vitest run server/src/lib/mixerMcp.test.ts`

## 明确不做
不加默认鉴权。不改 nginx。不把管线改成 MCP 编排。
