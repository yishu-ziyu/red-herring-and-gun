# 第 11 章施工规范 · 目标设定与监控

## status
verify-only

## 独立审查意见
四类停因、转载不触发重判、翻案续期笼子都在。
过满：true 且无 URL 不进循环，那是第 14、18 章。
维持 verify-only。

## Goal
已有测试全绿。产品代码零改动。

## Hard bar
evidenceLoop / evidencePursuit / runCasePipeline / faceVerdict / hops UI 已有测试全绿。

## 改哪些文件
无

## 测试
`npx vitest run server/src/lib/evidenceLoop/evidenceLoop.test.ts server/src/lib/evidencePursuit/evidencePursuit.test.ts server/src/lib/casePipeline/runCasePipeline.test.ts server/src/lib/reportAssembly/assembleFinalReport.test.ts src/lib/evidencePursuitUi.test.ts`（在 mvp/）

## 明确不做
不改生产代码。不让模型自报成功。不改类型闸、P2、问法、bind。
