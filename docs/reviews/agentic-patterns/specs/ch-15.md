# 第 15 章施工规范 · 智能体间通信（A2A）

## status
verify-only

## 独立审查意见
生产是同进程 JSON 交接，不是 Google A2A。
仓库有 UI AgentCard，不是协议身份文件。
没有「缺 A2A 导致没来源却写成能信」。维持 verify-only。

## Goal
交接测试绿。不上 Google A2A。

## Hard bar
buildAgentInput 透传 claimAtoms 与 merge 后的 subclaimVerdicts。
bundle.forAgent 带来源 URL。
rumorDetectorSchema.required 无 handoffTargets。
生产代码无 well-known/agent.json、sendTask、@a2a。

## 改哪些文件
无

## 测试
`npx vitest run server/src/lib/agentConfigs.test.ts server/src/lib/atomSearch.test.ts server/src/lib/reportAssembly/assembleFinalReport.test.ts server/src/lib/crossExam/crossExam.test.ts`（在 mvp/）

## 明确不做
不上 Google A2A。不删 UI AgentCard。
