# 第 16 章施工规范 · 资源感知优化

## status
implement

## 独立审查意见
MiniMax-M3 超时默认 600000ms，两次超时才 skip，冷启动可能空等很久。
modelOverride 不读 skip map。
第一次满超时本轮不改数字；只让超时一次后不再打 MiniMax。

## Goal
MiniMax 本进程超时一次之后，后续 callAgentWithFallback（含 override）不再打 MiniMax。
第一次仍等到 MINIMAX_M3_PROVIDER_TIMEOUT_MS。

## Hard bar
MINIMAX_M3_PROVIDER_TIMEOUT_MS=1，永不 resolve 的 MiniMax：第一次后返回 StepFun；第二次含 override 不再增加 MiniMax 调用。
B4-stepfun-3.7-timeout 仍能落到 step-2-mini。

## 改哪些文件
`mvp/server/src/lib/providerRouter.ts`：minimax 超时一次即 skipProvider；override 前若已 skip 则不 dispatch。
`providerRouter.test.ts`：Hard bar 两条，用 resetProviderQuotaSkipForTests。

## 测试
`npx vitest run server/src/lib/providerRouter.test.ts`（在 mvp/）

## 明确不做
不改默认 600000。不改检索策略。不改 fallbackAgentStep。不改 modelServiceHealth。
