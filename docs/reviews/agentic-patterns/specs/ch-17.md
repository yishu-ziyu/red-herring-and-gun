# 第 17 章施工规范 · 推理技术

## status
verify-only

## 独立审查意见
推理是拆开再逐条核，结果第一眼不是思考链。成立。
过满：stepFunReasoningEffortForModel 只管 flash；自证失败是 fail-open。
不推翻 INDEX 5 分。

## Goal
结果第一眼仍是能信/不能信。产品代码零改动。

## Hard bar
ResultView 无 ThinkingReasoning。
agent_thought 不进对用户说的话。
立场条「立场型 / 不适用真/假判断」。

## 改哪些文件
无

## 测试
`npx vitest run src/components/v3/phases/mission/ThinkingReasoning.test.tsx src/lib/missionShell/speechInstrument.test.ts src/components/v3/ReportModal.test.tsx server/src/lib/casePipeline/runCasePipeline.test.ts`（在 mvp/）

## 明确不做
不把思维链做成第一眼。不上 ReAct。不改类型闸、bind、eval。
