# 第 19 章施工规范 · 评估和监控

## status
implement

## 独立审查意见
24 条黄金案、unverified/true 各 0、幻觉分母为零、baseline 仍 14 条，与代码一致。
三类错该做。过满：不要改 TINY-005/006 期望、不要把 mixed→false 升格幻觉、不要本轮重写 baseline 假通过率。
mustSearch 看 atomsSearched，不要要求拆题 verifiable===true。

## Goal
评测能单独抓住：半真半假按条拆反、期望还查不清却写成能信或不能信、能核对的说法没进检索。
存在 RUMOR-011 按条期望、EVAL-UNVERIFIED-001、EVAL-TYPEGATE-001。
scoreCase 能读 atomSearchBundle.atomsSearched。
旧案无新字段时行为不变。

## Hard bar
`npx vitest run server/eval/score.test.ts` 全绿。
原子对调 → overallPass false。
原子正确且真侧有绑定 URL → 不因按条规则失败。
期望 unverified 却写成 false（0 URL）→ hallucination + fail。
期望 unverified 且实际 unverified（0 URL）→ pass。
mustSearch 未命中 → fail。
mustSearch 命中 → 不因类型闸规则失败。
compareToBaseline totalCases 14 vs 当前条数 → 失败。

## 改哪些文件
`mvp/server/eval/golden.ts`：可选 expectedAtoms、mustSearch；RUMOR-011 加按条；新增 EVAL-UNVERIFIED-001、EVAL-TYPEGATE-001。
`mvp/server/eval/score.ts`：读 bundle；按条匹配；mustSearch；compareToBaseline 先比 totalCases。
`mvp/server/eval/run.ts`：传入新字段。
`mvp/server/eval/score.test.ts`：Hard bar 各一条。
不改 baseline.json 造假通过。不改 runCase.ts。

## 测试
`cd mvp && npx vitest run server/eval/score.test.ts`

## 明确不做
不改生产管线。不加 LLM 评委。不改 TINY 期望。不把 eval:gate 接进 CI。
