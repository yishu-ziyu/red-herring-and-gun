# 第 21 章施工规范 · 探索和发现

## status
verify-only

## 独立审查意见
探索是有笼子的换问法。开放浏览故意不做。
过满：第一轮写成 true/false 时本章不会再换问法。
维持 verify-only。

## Goal
已有追索测试绿。产品代码零改动。

## Hard bar
六路问法、同站转载增益低于 0.18、无触发不跑循环、新证据才重判、立场条不检索。

## 改哪些文件
无

## 测试
`npx vitest run server/src/lib/evidencePursuit/evidencePursuit.test.ts server/src/lib/evidenceLoop/evidenceLoop.test.ts server/src/lib/atomSearch.retrieve.test.ts`（在 mvp/）

## 明确不做
不开开放浏览。不删 recursive-search 文档。不改问法主配方、bind、类型闸。
