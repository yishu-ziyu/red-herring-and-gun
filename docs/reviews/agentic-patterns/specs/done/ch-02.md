# 第 2 章验收记录 · 路由

## status
implement 已核

## 日期
2026-08-19

## 改了哪些文件
新建 `mvp/server/src/lib/claimAtom/forceCheckable.ts`：纯函数 `looksLikeCirculatingClaim` / `forceCheckableAtomTypes`。
新建 `mvp/server/src/lib/claimAtom/forceCheckable.test.ts`：Hard bar 三例 + 导致→causal + 不像流传说法。
改 `mvp/server/src/lib/claimAtom/index.ts`：导出闸函数。
改 `mvp/server/src/lib/casePipeline/runCasePipeline.ts`：自证之后、`retrieveForAtoms` 之前调用一次并写回 `claimAtomTypes`。
改 `mvp/server/src/lib/casePipeline/runCasePipeline.test.ts`：管线 Hard bar（隔夜菜仍检索，规范句与纯骂不检索）。
未改 `atomSearch.ts`、eval、LangGraph。

## 命令
在 `mvp/` 执行：
`npx vitest run server/src/lib/claimAtom/`
`npx vitest run server/src/lib/casePipeline/runCasePipeline.test.ts`
另跑：`npx vitest run server/src/lib/agentConfigs.test.ts -t "排除层"`（splitVerifiableAtoms 不变量）

## 过 / 失败
过。
vitest 4.1.7。
`claimAtom/`：5 通过，0 失败。
`runCasePipeline.test.ts`：14 通过，0 失败。
`agentConfigs.test.ts` 排除层：10 通过，42 跳过，0 失败。
Hard bar：`隔夜菜会致癌` 被标 `type=value` / `verifiable=false` 仍进 `searchOne` / `atomsSearched`；`政府应该禁止隔夜菜` 与 `这种政策就是不管老百姓死活` 不检索，进 `nonVerifiableAtoms`（报告该条「立场型 / 不适用真/假判断」）。
