# 第 8 章验收记录 · 记忆管理

## status
verify-only 已核

## 日期
2026-08-19

## 命令
在 `mvp/` 执行：`npx vitest run src/lib/semanticRecall.test.ts server/src/lib/memoryCandidateHandlers.test.ts server/src/lib/memoryCandidateGenerator.test.ts`

## 过 / 失败
过。
vitest 4.1.7。
3 文件通过，21 通过，0 失败。
`semanticRecall.test.ts` 8。
`memoryCandidateGenerator.test.ts` 3。
`memoryCandidateHandlers.test.ts` 10。

## proposed 是否进入 searchAccepted
不进入。
用例：`proposed 不进入 searchAccepted`。
只 `propose` 后 `searchAccepted("隔夜菜致癌")` 长度为 0。
`setStatus accepted` 后同查询可命中。
`mvp/server/src/lib/memoryCandidateStore.ts` 的 `searchAccepted` 先 `list({ status: "accepted" })`。
生成器写出的候选 `status` 全是 `proposed`。

## runCasePipeline 是否只有 propose
只有 propose。
`mvp/server/src/lib/casePipeline/runCasePipeline.ts` 结案调 `buildMemoryCandidatesFromRun`，再 `memoryCandidateStore.propose`。
该文件全文无 `searchAccepted`。
补跑：`npx vitest run server/src/lib/casePipeline/runCasePipeline.test.ts -t "proposes memory candidates"` 1 通过，12 跳过。
`searchAccepted` 只出现在 `mvp/src/lib/agentRuntime/AgentRuntime.ts`，那是 Dev/eval，不是生产 Case Pipeline。

## MemoryCandidatePanel 除自身外是否有 import
无。
全仓库无 `import { MemoryCandidatePanel`。
`ResultView.tsx` 现无此 import。
`MissionControlView.tsx` 无此标识符。
确认按钮归第 13 章，本章不挂。

## 旧案是否当本案证据
不当。
生产开案不调 `searchAccepted`。
客户端 `buildLocalMemoryRecall` 只 `listMemoryCandidates({ status: "accepted" })`。
旧案 URL 标成「线索」，文案写不直接进入本案结论。
`handlers.ts` 的 `normalizeClientMemoryRecall` 写死 policy：不得把旧案结论直接当作本案证据。

## 非本章（不挡验收）
确认按钮未挂结果页，归第 13 章；第 13 章可能改 `ResultView`。
问法复用归第 9 章。
无网址不得写成能信 / 不能信，归 bind。

## 产品代码
零改动。
未改记忆模块。
未改 `ResultView`。
