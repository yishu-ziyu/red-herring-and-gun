# 第 12 章验收记录 · 异常处理与恢复

## status
implement 已做

## 日期
2026-08-19

## 命令
在 `mvp/` 执行：`npx vitest run server/src/lib/casePipeline/runCasePipeline.test.ts`

## 过 / 失败
过。
vitest 4.1.7。
13 通过，0 失败。
改写测例：`fact_checker 与 source_validator 失败时即使检索有辟谣链接也只能 unverified`。
断言成立：`factCheckResult` 与 `sourceReliability` 均为 `unverified`；`counterEvidence` 空；`verifiedSources` 空；辟谣 URL 仍在 `fact_checker.sources`。

## 产品代码
只改 `mvp/server/src/lib/casePipeline/runCasePipeline.ts` 的 `fallbackAgentStep`。
核查 / 信源模型未完成时固定 `unverified`，不再用 `isOnTopicDebunk` 写 `false` 或 `medium`。
`counterEvidence` 置空。
`verifiedSources` 置空。
检索 URL 仍写入 `fact_checker.sources`，不当成已经查完。
未改 `boundTinyRumorVerdict` 及其调用。
未改 `atomSearchQuery`、bind、handlers、providerRouter。

## 残余
对题辟谣链接时，报告脸仍可能被 `boundTinyRumorVerdict` 改成「不能信」。
本章不许动那条路。
失败兜底本身不再写成不能信。
