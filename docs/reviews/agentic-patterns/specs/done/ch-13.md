# 第 13 章验收 · 人机协同

## status
done

## Hard bar
dossier 结果页对本案 proposed 显示「写入知识库」和「忽略」，判断词在面板上方。
点写入后 POST setStatus accepted，且 `listMemoryCandidates({ status: "accepted" })` 含该 id。
点忽略后 rejected，accepted 列表不含该 id。
未确认的 proposed 不在 accepted。
中断页不显示写入知识库。
其他 claim 的候选不出现在本页。

## 跑了什么
`cd mvp && npx vitest run src/components/v3/phases/ResultView.test.tsx`
14 passed。

## 改了什么
`mvp/src/components/v3/phases/ResultView.tsx`：仅 `variant="dossier"` 挂已有 `MemoryCandidatePanel`。
用 `createKnowledgeBase()` 列出 `provenance.claim` 等于本案的候选。
确认/忽略先 `updateMemoryCandidateStatus` 再 `saveMemoryCandidate`。
`mvp/src/components/v3/phases/ResultView.test.tsx`：补齐 Hard bar 用例。
`mvp/src/components/v3/MemoryCandidatePanel.tsx`：去掉用户看得见的「Agent」字样。
`mvp/src/styles.css`：`.result-view .memory-candidate-panel` 增加 8px 上间距。

## 没改
不改 MissionControlView onComplete。
不恢复指挥台。
不改 knowledgeBase / memoryCandidateStore / atomSearchQuery / runCasePipeline。

## 残余风险
下一案召回仍只读本地 `knowledgeBase` 的 `accepted`。
若结案没有把本案 proposed 写入本地知识库，结果页不会出现按钮。
API 失败时面板保持 proposed，本页没有另做错误条。
