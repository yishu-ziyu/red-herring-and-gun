# 第 16 章验收记录 · 资源感知优化

## status
implement 已核

## 日期
2026-08-19

## 命令
在 `mvp/` 执行：`npx vitest run server/src/lib/providerRouter.test.ts`

## 过 / 失败
过。
vitest 4.1.7。
27 通过，0 失败。
含新增「MiniMax 超时一次后本进程含 override 不再打」。
含原有 `B4-stepfun-3.7-timeout`。

## 默认超时
未改。
`timeoutForProviderModel` 对 MiniMax-M3 仍 `getTimeoutMs(..., 600000)`。
第一次超时仍等到 `MINIMAX_M3_PROVIDER_TIMEOUT_MS`。
新测试只在用例 env 里设 `MINIMAX_M3_PROVIDER_TIMEOUT_MS=1`，不改缺省。

## MiniMax 超时一次即 skip
`noteProviderFailure` 对 `minimax` 超时 1 次即 `skipProvider`。
其他家仍 2 次才 skip。
`callAgentWithFallback` 在 modelOverride 前读 skip map；已 skip 则不 `dispatchSingleProvider`。
永不 resolve 的 MiniMax：第一次落到 StepFun；第二次带 MiniMax override，调用次数仍是 1。

## B4-stepfun-3.7-timeout
仍落到 `step-2-mini`。
StepFun 仍是两次超时才 skip，override 超时后同一次调用会试默认模型。

## 产品代码
`mvp/server/src/lib/providerRouter.ts`
`mvp/server/src/lib/providerRouter.test.ts`
未改 `fallbackAgentStep`、`modelServiceHealth`、检索策略。
