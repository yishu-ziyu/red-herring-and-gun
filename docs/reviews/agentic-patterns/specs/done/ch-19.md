# 第 19 章验收记录 · 评估和监控

## status
implement 已做

## 日期
2026-08-19

## Goal
评测能单独抓住三类错：半真半假按条拆反、期望还查不清却写成能信或不能信、能核对的说法没进检索。
存在 RUMOR-011 按条期望、EVAL-UNVERIFIED-001、EVAL-TYPEGATE-001。
scoreCase 读 atomSearchBundle.atomsSearched。
旧案无新字段时行为不变。

## Hard bar
在 `mvp/` 执行：`npx vitest run server/eval/score.test.ts`

## 过 / 失败
过。
vitest 4.1.7。
22 通过，0 失败。
原子对调 → overallPass false。
原子正确且真侧有绑定 URL → 不因按条规则失败。
期望 unverified 却写成 false（0 URL）→ hallucination + fail。
期望 unverified 且实际 unverified（0 URL）→ pass。
mustSearch 未命中 → fail。
mustSearch 命中（即使拆题写成立场型）→ 不因类型闸规则失败。
compareToBaseline totalCases 14 vs 当前条数 → 失败。

## 改动
`mvp/server/eval/golden.ts`：可选 `expectedAtoms` / `mustSearch`；RUMOR-011 加按条；新增 EVAL-UNVERIFIED-001、EVAL-TYPEGATE-001。
`mvp/server/eval/score.ts`：读 bundle；按条匹配；mustSearch；`compareToBaseline` 先比 totalCases。
`mvp/server/eval/run.ts`：把新字段和 bundle 传入 scoreCase；门禁用 compareToBaseline。
`mvp/server/eval/score.test.ts`：Hard bar 各一条，并钉 related-only 不算绑定、mixed→false 不算幻觉、旧案行为不变。

## 明确未做
不改生产管线。
不改 `runCase.ts`。
不改 `baseline.json`。
不改 TINY-005 / TINY-006 期望。
不把 mixed→false 升格幻觉。
不加 LLM 评委。
不把 eval:gate 接进 CI。

## 产品代码
只改 `mvp/server/eval/**`（不含 `runCase.ts`）。

## 残留
`baseline.json` 仍是 14 条旧聚合。
全量 `--gate` 会因 totalCases 不一致失败，这是本轮故意留下的门。
全量跑且不加 `--gate` 仍会按旧逻辑覆写 baseline.json。
