# 第 20 章验收记录 · 优先级排序

## status
implement 已做

## 日期
2026-08-19

## Goal
7 条可核查时，带因果或数字的那条必须进入 6 个检索名额。
没进名额的可核查条仍按原句序出现，且只能是 unverified。
补查 3 个名额先给 conflict。
上限 6 与 3 不变。

## Hard bar
在 `mvp/` 执行：
`npx vitest run server/src/lib/atomSearch.test.ts server/src/lib/atomSearch.retrieve.test.ts server/src/lib/reportAssembly/assembleFinalReport.test.ts server/src/lib/evidenceLoop/evidenceLoop.test.ts server/src/handlers.reportFallback.test.ts`

## 过 / 失败
过。
上述文件 + reviewer / agentConfigs / runCasePipeline 合计 142 通过，0 失败。
7 条可核查、第 7 条含「导致」→ `searchOne` 6 次且含第 7 条。
claimItems 原句序；未入选条 unverified，gaps 含「检索预算未覆盖」。
4 条补查候选、前 3 unverified、第 4 conflict → 先收 conflict，长度 3。
第 7 条立场型仍按原句序出现，不进 `subclaimVerdicts`。

## 改动
`mvp/server/src/lib/atomSearch.ts`：`listAtomsForSearch` 先看最多 12 条；`selectAtomsToSearch` 按 causal/导致 > 数字 > 其余排序再取 6；`retrieveForAtoms` 用全表打分；未入选条 `applyUnsearchedAtomVerdicts`。
`mvp/server/src/lib/reportAssembly/assembleFinalReport.ts`：merge 只用 `atomsSearched`；未检索可核查条补 unverified。
`mvp/server/src/lib/evidenceLoop/evidenceLoop.ts`：`findLoopTargets` 先 conflict 后 unverified。
`mvp/server/src/lib/claimAtom/selfProof.ts`：预过滤从 6 提到 12，否则第 7 条到不了打分。
对应测试：atomSearch / retrieve / assemble / evidenceLoop / reportFallback / prefilter / runCasePipeline 的原先期望。

## 明确未做
未改类型闸 `forceCheckable`。未改 `atomSearchQuery`。未改 bind。未无限搜。未把未检索条送进补查。
检索上限仍是 6，补查上限仍是 3。

## 残留
`splitVerifiableAtoms` / `mergeSubclaimVerdicts` 内部仍 `compactStrings(..., 6)`。assemble 已绕开。
其它直接调用 split 的旧路径仍会先切 6。
浏览器 AgentRuntime 不走这条生产排序。
