# 第 21 章验收记录 · 探索和发现

## status
verify-only 已核

## 日期
2026-08-19

## 命令
在 `mvp/` 执行：`npx --no-install vitest run server/src/lib/evidencePursuit/evidencePursuit.test.ts server/src/lib/evidenceLoop/evidenceLoop.test.ts server/src/lib/atomSearch.retrieve.test.ts`

## 过 / 失败
过。
vitest 4.1.7。
3 文件，37 通过，0 失败。
`evidencePursuit.test.ts` 12 通过。
`evidenceLoop.test.ts` 23 通过。
`atomSearch.retrieve.test.ts` 2 通过。

## Hard bar
六路问法：`buildQueryPortfolio` 对带日期产品句依次发出 exact / entity / primary / temporal / refutation / alternative。
同站转载增益低于 0.18：`GAIN_STOP_THRESHOLD` 为 0.18；官方新 host 过线，同站转载 `gain < 0.18` 且 `newNonReprint === 0`。
循环侧：同站转载不立刻停，继续下一问；命中原始来源才 `evidence-found`。
无触发不跑循环：正向判词（含空来源）不进 `findLoopTargets`；已有单侧来源的结论也不进。
无触发时 `runEvidenceLoop` 返回 `ran: false`，`searchOne` 零次。
新证据才重判：第一轮新证据 → `stopReason === "evidence-found"` 且 `recheckFactChecker === true`。
两轮零新增 → `no-new-evidence` 且 `recheckFactChecker === false`。
接线：`runCasePipeline` Phase 2a 在 `!passOutcome.recheckFactChecker` 时直接停，不重跑 `fact_checker`。
立场条不检索：`retrieveForAtoms` 对 `verifiable: false` 的「立场」不调 `searchOne`；`atomsSearched` 只有「事实1」「事实2」。
报告文案仍是「立场型 / 不适用真/假判断」。

## 明确不做（已核）
`runCasePipeline` 不调用 `requestRecursiveSearch`。
`mvp/docs/RECURSIVE_EVIDENCE_SEARCH_PLAN.md` 仍在仓库。
本轮未改问法主配方、bind、类型闸。

## 产品代码
零改动。
