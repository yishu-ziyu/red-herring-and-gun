#!/usr/bin/env bash
# Retired. This used to pack with --exclude dist and a global *.png exclude.
echo "Retired. Live deploy is: ./ops.sh deploy --yes" >&2
echo "Run it from the repo root. Do not use this script." >&2
exit 1
